# Alacritty Setup

1. Create `~/.config/alacritty/themes`
2. Download theme file into `themes` directory
3. Import the theme in your `~/.config/alacritty/alacritty.toml`

### Example

```toml
[general]
import = ["~/.config/alacritty/themes/oasis_lagoon.toml"]
```
