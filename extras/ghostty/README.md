# Ghostty Setup

1. Create `~/.config/ghostty/themes`
2. Download theme file into `themes` directory
3. Choose the theme in `~/.config/ghostty/config`

### Example

```ini
theme = "oasis_lagoon"
```

> [!TIP]
> To see the full list of your installed themes: `ghostty +list-themes`
